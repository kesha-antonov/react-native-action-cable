# Copilot Instructions

## Package Manager

Always use **yarn** for package management commands:

- Install packages: `yarn add <package>`
- Install dev dependencies: `yarn add -D <package>`
- Install all dependencies: `yarn install` or `yarn`
- Run scripts: `yarn <script-name>`

Do not use npm commands.
