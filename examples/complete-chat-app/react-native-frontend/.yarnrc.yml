nodeLinker: node-modules

# @kesha-antonov/react-native-chat is maintained alongside this library, so its
# releases are picked up as soon as they ship instead of waiting out Yarn's
# minimum release age. Everything else keeps the default cooldown.
npmPreapprovedPackages:
  - "@kesha-antonov/react-native-chat"
